{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "esbuild": "esbuild ./src/app.js --bundle --minify --sourcemap --loader:.js=jsx --outfile=./public/js/out.js --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.17.15",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
